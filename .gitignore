node_modules/
dist/
build/
coverage/
.vite/

# env — never commit secrets
.env
.env.*
!.env.example

# local sqlite database
*.db
*.db-journal
*.sqlite
apps/api/prisma/*.db*

# uploads (local file-storage adapter)
apps/api/uploads/

# logs
*.log
npm-debug.log*

# per-developer Claude Code settings (.claude/settings.json is shared, this one is not)
.claude/settings.local.json

# os / editor
.DS_Store
.idea/
*.swp
