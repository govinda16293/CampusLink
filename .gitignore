node_modules/
dist/
build/
coverage/
.vite/

# env — never commit secrets
.env
.env.*
!.env.example

# local sqlite database
*.db
*.db-journal
*.sqlite
code/backend/prisma/*.db*

# uploads (local file-storage adapter)
code/backend/uploads/

# logs
*.log
npm-debug.log*

# per-developer Claude Code settings (.claude/settings.json is shared, this one is not)
.claude/settings.local.json

# os / editor
.DS_Store
.idea/
*.swp

# Scratch folder for design uploads. Anything the app actually uses is copied into
# code/frontend/src/assets/ and committed from there, so the original drop stays local.
input/
