/**
 * Public surface of @campuslink/shared.
 *
 * Imported by both `apps/api` (TypeScript, via allowJs) and `apps/web` (plain JavaScript).
 * Keep everything in here runtime-safe and framework-free — no Express, no React, no Prisma.
 */

export * from './enums.js';
export * from './schemas/auth.js';
export * from './schemas/profile.js';
