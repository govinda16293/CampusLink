[project]
name = "campuslink-docs"
version = "0.1.0"
description = "Documentation site for the CampusLink UCS503P project."
requires-python = ">=3.10"
dependencies = []

[project.optional-dependencies]
docs = [
  "mkdocs>=1.6.0",
  "mkdocs-material>=9.5.0"
]

